# Integration Setup

This directory holds stable setup artifacts for the first supported happy path.

Current contents:

- `happy-path-workflow.json`: references the fixture manifest entry and the contract example chain used by integration validation.
