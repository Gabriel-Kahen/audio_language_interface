# Contract Examples

This directory is reserved for example payloads that demonstrate how modules communicate.

Each example should:

- correspond to a published schema in `contracts/schemas/`,
- be minimal but realistic,
- include field-level comments in adjacent documentation when the format allows it,
- and be updated whenever the contract changes.
