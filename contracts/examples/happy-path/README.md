# Happy-Path Examples

This directory captures the first coherent Phase 1 workflow slice around the prompt:

`make this loop darker and less harsh without losing punch`

It complements the top-level schema-validated examples by adding cross-step setup artifacts that span:

- fixture provenance
- semantic interpretation
- candidate post-transform analysis
- session lineage
- ordered tool invocation references

The workflow entrypoint is `workflow.json`.
